#include "main.h"
/**
 * mul - Enrty point
 * @a: number to be multipled
 * @b: also to be multiplied
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	int mult;

	mult = a * b;
	_putchar(mult + '0');
	return (0);
}
