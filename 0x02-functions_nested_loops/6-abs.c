#include "main.h"
/**
 * _abs(int) - Entry point
 * @n: he integer to be computed.
 * Return: The absolute value of the integer.
 */
int _abs(int)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
