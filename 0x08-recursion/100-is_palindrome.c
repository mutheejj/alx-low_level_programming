#include "main.h"
#include <string.h>
/**
 * is_palindrome - returns 1 if a string is a palindrome and 0 if not
 * @s: is te string
 * Return: 0 or 1
 */
int is_palindrome(char *s)
{
	int i = strlen(s);

	if (i <= 1)
	{
		return (1);
	}
	return (1);
}
