#include "main.h"
/**
 * wildcmp - ompares two strings returns 0 or 1
 * @s1: a string
 * @s2: a string
 * Return: 1 or 0
 */
int wildcmp(char *s1, char *s2)
{
	if (*s1 == '\0' && *s2 == '\0')
	{
		return (1);
	}
	return (1);
}
